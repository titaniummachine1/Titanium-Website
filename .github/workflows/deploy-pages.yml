name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Init engine submodule
        run: git -c submodule.recurse=false submodule update --init --depth 1 engine

      - uses: dtolnay/rust-toolchain@nightly
        with:
          targets: wasm32-unknown-unknown
          components: rust-src

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Build WASM + static site
        working-directory: web
        env:
          VITE_BUILD_SHA: ${{ github.sha }}
          # wasm32 cross-compile — do not use target-cpu=native (native titanium.exe only).
          RUSTFLAGS: ""
          # Threaded build: play multithreads when crossOriginIsolated (COI
          # service worker). If the threaded search crashes on a weaker device,
          # titaniumWasmClient degrades that session to single-thread and retries
          # — "multithreading if possible", never a hard crash. The CAT overlay
          # worker uses this same build single-threaded (no initThreadPool).
          TITANIUM_WASM_THREADS: "1"
        run: |
          npm ci
          npm run build:pages

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

      - id: deployment
        uses: actions/deploy-pages@v4
